/**
 * Thin agent client: keep capture + input; hide debug / local-mapping scaffolding.
 * Controller / vision model lives on a separate Web (or Python) client over TCP.
 */
export const THIN_CLIENT = true
