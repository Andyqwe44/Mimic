---
description: PC C++ 后端 — LOG、命令、peer、更新
globs: pc/**/*.{cpp,h,hpp},shared/protocol/**/*.h
alwaysApply: false
---

# PC C++ backend (`pc/`)

## 日志

只用 `LOG` / `LOG_WARN` / `LOG_ERROR` / `LOG_DEBUG`（`pc/logger`）。禁止裸打印。

生产默认 `LOG_LEVEL_INFO`。

## Settings

AppData：`%LOCALAPPDATA%\MimicClient\`（config / log / staging / WebView2）。

## 角色

本机 `pc/client` = **捕获 + 执行**。信令默认 Bootstrap `http://47.107.43.5:8443`。

`crash_log` hostCall：前端未捕获异常 → `LOG_ERROR("crash", ...)`。

铁律 5：不静默回退方法；失败返回 error JSON。
