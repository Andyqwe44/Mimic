---
description: shared/web 前端 — Tooltip、design tokens、i18n、跨端 bridge
globs: shared/web/**/*.ts,shared/web/**/*.tsx,shared/web/**/*.css
alwaysApply: false
---

# shared/web

PC WebView2 与 Android Capacitor **共用**同一套 React UI。

## Bridge

- Windows：`chrome.webview.postMessage`（`bridge.ts`）
- Android：`Capacitor.Plugins.MimicHost.call`（同 `hostCall` API）
- 平台探测：`lib/platform.ts`

## UI 规则

- **禁止原生 HTML `title`** — 只用 `<Tooltip text="...">`
- i18n：`locales/`；插值 `{var}`
- DevMode overlay / SSOT：关 Dev = 清 overlay；假更新弹窗不调下载 API

## Android skeleton

暂不实现捕获/输入；不支持的 `hostCall` 由 MimicHost 返回明确 error。崩溃 / probe / APK 更新优先。
