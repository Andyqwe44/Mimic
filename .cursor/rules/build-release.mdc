---
description: 构建与发版（PC / Server / Android CDN）
globs: scripts/**/*.ps1,installer/**/*.iss,release/**/*.json,pc/client/src/version.h,server/package.json,android/version.json,docs/auto-update.md
alwaysApply: false
---

# Build & Release

## 版本

| 产品 | 真相源 |
|------|--------|
| MimicClient (PC) | `pc/client/src/version.h` → `APP_VERSION` |
| MimicServer | `server/package.json` → `version` |
| Mimic Android | `android/package.json` + `android/version.json` |

## 构建

```powershell
cd shared\web; npm run build
powershell -File scripts\Build.ps1
powershell -File scripts\Build.ps1 -Module mimic_client
```

输出：`pc/client/build/{bin,frontend,config}`（exe = `mimic_client.exe`）。

## 一键发布

```powershell
# bump version.h 和/或 server/package.json 后:
powershell -File scripts\Release.ps1
powershell -File scripts\Release.ps1 -ClientOnly
powershell -File scripts\Release.ps1 -ServerOnly
powershell -File scripts\Release.ps1 -DryRun
```

Android APK 通道：上传到 CDN `http://47.107.43.5/mimic/android/`（`version.json` + apk）；详见 `android/README.md`。

CDN client/server：`scripts/Publish-Cdn.ps1`。
